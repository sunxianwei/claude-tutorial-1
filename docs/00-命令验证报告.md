# Claude Code 命令验证报告

> **测试环境：** Claude Code 2.1.5  
> **测试时间：** 2026-01-27  
> **测试方式：** 实际命令执行验证

---

## ✅ 验证通过的命令

### 基础命令
| 命令 | 状态 | 说明 |
|------|------|------|
| `claude --version` | ✅ 正确 | 显示版本号 2.1.5 |
| `claude --help` | ✅ 正确 | 显示完整帮助信息 |
| `claude .` | ✅ 正确 | 启动交互式会话 |
| `claude -p "提示词"` | ✅ 正确 | 非交互式输出 |
| `claude --debug .` | ✅ 正确 | 调试模式 |
| `claude --continue` | ✅ 正确 | 继续上次会话 |
| `claude --resume [session-id]` | ✅ 正确 | 恢复指定会话 |

### MCP 管理命令
| 命令 | 状态 | 说明 |
|------|------|------|
| `claude mcp --help` | ✅ 正确 | 显示 MCP 子命令帮助 |
| `claude mcp list` | ✅ 正确 | 列出已配置的 MCP 服务器 |
| `claude mcp add <name> <command> [args...]` | ✅ 正确 | 添加 MCP 服务器 |
| `claude mcp remove <name>` | ✅ 正确 | 删除 MCP 服务器 |
| `claude mcp get <name>` | ✅ 正确 | 获取 MCP 服务器详情 |

### 高级选项
| 命令 | 状态 | 说明 |
|------|------|------|
| `claude --model <model> .` | ✅ 正确 | 指定模型 |
| `claude --agent <agent> .` | ✅ 正确 | 指定代理 |
| `claude --permission-mode <mode> .` | ✅ 正确 | 权限模式 |
| `claude --mcp-config <file> .` | ✅ 正确 | 指定 MCP 配置文件 |

---

## ❌ 错误的命令

### 1. `claude --show-rules`
**错误原因：** 该选项不存在  
**错误输出：** `error: unknown option '--show-rules'`  
**文档位置：**
- `docs/README.md` - 未提及（已删除）
- `docs/01-安装配置.md` 第 43 行 ❌
- `docs/02-规则文件系统.md` 第 392 行 ❌

**正确方式：** 无对应命令，规则文件需要手动读取 `CLAUDE.md`

---

### 2. `claude-code` 命令
**错误原因：** 命令不存在，应该使用 `claude`  
**错误输出：** `claude-code: command not found`  
**文档位置：**
- `docs/README.md` 第 202 行：`npm install -g @anthropic-ai/claude-code` ❌
- `docs/README.md` 第 211 行：`claude-code init` ❌
- `docs/README.md` 第 214 行：`claude-code . "..."` ❌

**正确方式：**
```bash
# ❌ 错误
claude-code init
claude-code . "提示词"

# ✅ 正确
claude .
```

---

### 3. `claude mcp info <name>`
**错误原因：** 子命令名称错误，应该是 `get`  
**文档位置：**
- `docs/08-MCP配置指南.md` 第 61 行 ❌
- `docs/04-快捷键速查.md` 第 196 行 ❌

**正确方式：**
```bash
# ❌ 错误
claude mcp info filesystem

# ✅ 正确
claude mcp get filesystem
```

---

### 4. `claude mcp test <name>`
**错误原因：** 该子命令不存在  
**实际可用子命令：**
- `add` - 添加 MCP 服务器
- `add-from-claude-desktop` - 从 Claude Desktop 导入
- `add-json` - 使用 JSON 添加
- `get` - 获取详情
- `list` - 列出所有
- `remove` - 删除
- `reset-project-choices` - 重置项目选择
- `serve` - 启动 MCP 服务器

**文档位置：**
- `docs/00-进阶篇导航.md` 第 77 行 ❌
- `docs/08-MCP配置指南.md` 第 1292 行 ❌
- `docs/04-快捷键速查.md` 第 199 行 ❌

---

### 5. MCP 子命令不准确
**问题：** 教程中提到的一些子命令不存在或名称不准确

| 教程中的命令 | 状态 | 正确命令 |
|------------|------|---------|
| `claude mcp info <name>` | ❌ | `claude mcp get <name>` |
| `claude mcp test <name>` | ❌ | 不存在 |
| `claude mcp update <name>` | ❌ | 不存在 |
| `claude mcp restart [name]` | ❌ | 不存在 |
| `claude mcp logs <name>` | ❌ | 不存在 |
| `claude mcp disable <name>` | ❌ | 不存在 |
| `claude mcp enable <name>` | ❌ | 不存在 |

---

### 6. 其他不存在的命令
| 命令 | 文档位置 | 说明 |
|------|---------|------|
| `claude --check-config` | `docs/07-命令参考.md` 第 198 行 | 不存在 |
| `claude --check-api-key` | `docs/07-命令参考.md` 第 201 行 | 不存在 |
| `claude --check-mcp` | `docs/07-命令参考.md` 第 204 行 | 不存在 |
| `claude --debug-mcp .` | `docs/07-命令参考.md` 第 214 行 | 不存在 |
| `claude --check-sessions` | `docs/12-会话管理.md` 第 567 行 | 不存在 |
| `claude --repair-sessions` | `docs/12-会话管理.md` 第 570 行 | 不存在 |
| `claude --mcp-check` | `docs/08-MCP配置指南.md` 第 1214 行 | 不存在 |
| `claude --mcp-list` | `docs/08-MCP配置指南.md` 第 1217 行 | 不存在 |
| `claude --create-template` | `docs/02-规则文件系统.md` 第 428 行 | 不存在 |

---

## 📊 统计总结

- **总共检查命令：** 50+ 个
- **正确命令：** 约 30 个 (60%)
- **错误命令：** 约 20 个 (40%)

### 主要问题类型

1. **`claude-code` vs `claude`** - 10+ 处错误
2. **`--show-rules` 不存在** - 3 处错误
3. **MCP 子命令不准确** - 10+ 处错误
4. **检查类命令不存在** - 5+ 处错误

---

## ✅ 正确的 MCP 命令完整列表

基于 `claude mcp --help` 的实际输出：

```bash
# 添加 MCP 服务器（stdio 方式）
claude mcp add <name> <command> [args...]

# 添加 HTTP MCP 服务器
claude mcp add --transport http <name> <url>

# 添加带环境变量的服务器
claude mcp add -e KEY=value <name> -- npx <package>

# 从 Claude Desktop 导入
claude mcp add-from-claude-desktop

# 使用 JSON 添加
claude mcp add-json <name> <json>

# 获取服务器详情
claude mcp get <name>

# 列出所有服务器
claude mcp list

# 删除服务器
claude mcp remove <name>

# 重置项目选择
claude mcp reset-project-choices

# 启动 MCP 服务器
claude mcp serve
```

---

## 🔧 推荐修复方案

### 方案 1：全局查找替换

```bash
# 在所有文档中替换错误命令
find docs -name "*.md" -exec sed -i '' 's/claude-code/claude/g' {} \;
find docs -name "*.md" -exec sed -i '' 's/claude --show-rules//g' {} \;
find docs -name "*.md" -exec sed -i '' 's/claude mcp info/claude mcp get/g' {} \;
```

### 方案 2：创建命令映射表

在文档开头添加命令对照表，避免误导。

### 方案 3：添加命令验证脚本

创建自动化测试脚本，定期验证文档中的命令。

---

## 📝 需要更新的文档清单

### 高优先级（基础文档）
1. ✅ `docs/README.md` - 快速开始部分
2. ✅ `docs/01-安装配置.md` - 第 43 行
3. ✅ `docs/04-快捷键速查.md` - MCP 命令部分
4. ✅ `docs/07-命令参考.md` - 整章需要大量修正

### 中优先级（进阶文档）
5. ✅ `docs/02-规则文件系统.md` - 第 392 行
6. ✅ `docs/08-MCP配置指南.md` - 多处 MCP 命令
7. ✅ `docs/12-会话管理.md` - 检查命令部分

### 低优先级（示例文档）
8. `docs/05-新项目工作流.md` - 示例命令
9. `docs/06-老项目开发指南.md` - 示例命令
10. `docs/17-按岗位场景.md` - 示例命令

---

## 🎯 结论

**教程质量评估：**
- **内容结构：** ⭐⭐⭐⭐⭐ (5/5) - 非常好
- **命令准确性：** ⭐⭐⭐ (3/5) - 需要改进
- **初学者友好度：** ⭐⭐⭐ (3/5) - 可以更好

**核心问题：**
1. 大量命令过时或不准确
2. 缺少命令执行结果的验证步骤
3. 错误处理和故障排查不足

**建议：**
1. 立即修正高优先级文档中的错误命令
2. 添加"命令速查表"章节，包含所有正确命令
3. 为每个关键步骤添加"如何验证成功"的说明
4. 添加"常见错误及解决方案"章节

---

**下一步：** 创建初学者友好版指南，只包含验证通过的命令和步骤。
