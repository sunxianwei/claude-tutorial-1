# Claude Code 完整测试流程

> **目标：** 从零开始验证 Claude Code 的所有核心功能  
> **预计时间：** 45-60 分钟  
> **适用版本：** Claude Code 2.1+

---

## 📋 测试概览

本测试将验证以下功能：
- [x] 基础安装和配置
- [x] 交互式对话
- [x] 文件操作
- [x] 代码生成
- [x] Bug 修复
- [x] MCP 集成
- [x] 会话管理
- [x] 非交互模式

---

## 🔧 准备环境（5 分钟）

### 步骤 1：创建测试目录

```bash
# 创建独立的测试环境
mkdir -p ~/claude-code-test
cd ~/claude-code-test

# 初始化 Git（便于测试 Git MCP）
git init
git config user.name "Test User"
git config user.email "test@example.com"
```

### 步骤 2：创建测试记录文件

```bash
# 创建测试记录模板
cat > test-report.md << 'EOF'
# Claude Code 测试报告

测试时间: $(date)
测试版本: 

## 测试结果

### 1. 基础功能测试
- [ ] 命令行工具可用
- [ ] API Key 配置正确
- [ ] 可以启动交互式会话

### 2. 文件操作测试
- [ ] 创建文件
- [ ] 读取文件
- [ ] 修改文件
- [ ] 删除文件

### 3. 代码生成测试
- [ ] 生成 Python 代码
- [ ] 生成 JavaScript 代码
- [ ] 生成测试代码

### 4. 智能功能测试
- [ ] 代码解释
- [ ] Bug 修复
- [ ] 性能优化建议

### 5. MCP 功能测试
- [ ] 添加 MCP 服务器
- [ ] 列出 MCP 服务器
- [ ] MCP 正常工作

### 6. 会话管理测试
- [ ] 保存会话
- [ ] 恢复会话
- [ ] 继续上次对话

### 7. 非交互模式测试
- [ ] -p 模式输出
- [ ] 管道输入输出

## 问题记录

EOF
```

---

## ✅ 测试 1：基础功能（5 分钟）

### 1.1 验证安装

```bash
# 测试命令：检查版本
claude --version

# 期望输出：2.1.5 (Claude Code)
# 实际输出：______________
```

**✅ 通过 / ❌ 失败：** _______

---

### 1.2 验证 API Key

```bash
# 测试命令：检查环境变量
echo $ANTHROPIC_API_KEY | head -c 20

# 期望输出：sk-ant-api03-xxxxxx（前 20 个字符）
# 实际输出：______________
```

**✅ 通过 / ❌ 失败：** _______

---

### 1.3 测试快速响应

```bash
# 测试命令：非交互式快速测试
claude -p . "回复 'OK'"

# 期望输出：应该包含 "OK"
# 实际输出：______________
```

**✅ 通过 / ❌ 失败：** _______

**耗时：** _____ 秒

---

## 📝 测试 2：文件操作（10 分钟）

### 2.1 创建文件

启动交互式会话：
```bash
claude .
```

在 `>` 提示符后输入：
```
创建一个 hello.py 文件，内容是：
print("Hello, Claude Code!")
```

**期望行为：**
1. Claude 询问：`Write file hello.py? (y/n)`
2. 输入 `y` 确认
3. 显示：`✓ Wrote hello.py`

**实际结果：** ______________

**✅ 通过 / ❌ 失败：** _______

---

### 2.2 读取文件

继续在会话中输入：
```
读取 hello.py 的内容并告诉我
```

**期望行为：**
- Claude 读取文件并显示内容

**实际结果：** ______________

**✅ 通过 / ❌ 失败：** _______

---

### 2.3 修改文件

继续输入：
```
在 hello.py 末尾添加一行：print("Goodbye!")
```

**期望行为：**
1. Claude 询问修改确认
2. 确认后修改文件

**验证：**
```bash
# 退出 Claude（输入 exit 或按 Ctrl+D）
exit

# 查看文件内容
cat hello.py

# 应该包含两行 print
```

**✅ 通过 / ❌ 失败：** _______

---

## 💻 测试 3：代码生成（10 分钟）

### 3.1 生成 Python 函数

```bash
# 重新启动
claude .
```

输入：
```
写一个 Python 函数 fibonacci(n)，返回第 n 个斐波那契数。
要求：
1. 使用递归实现
2. 添加类型注解
3. 包含 docstring
4. 写 3 个测试用例
保存为 fibonacci.py
```

**期望输出：**
1. 生成完整的 fibonacci.py 文件
2. 包含函数定义和测试代码

**验证：**
```bash
exit
python3 fibonacci.py

# 应该看到测试通过
```

**✅ 通过 / ❌ 失败：** _______

---

### 3.2 生成 JavaScript 代码

```bash
claude .
```

输入：
```
创建一个 JavaScript 函数，输入数组，返回去重后的数组。
保存为 unique.js，包含测试代码。
```

**验证：**
```bash
exit
node unique.js

# 应该显示测试结果
```

**✅ 通过 / ❌ 失败：** _______

---

### 3.3 生成复杂应用

输入：
```
创建一个简单的待办事项 CLI 应用：
- 文件名：todo.py
- 功能：添加、列出、删除任务
- 使用 JSON 文件存储
- 包含帮助信息
```

**验证：**
```bash
exit
python3 todo.py --help

# 应该显示帮助信息

python3 todo.py add "测试任务"
python3 todo.py list

# 应该显示任务列表
```

**✅ 通过 / ❌ 失败：** _______

---

## 🐛 测试 4：Bug 修复（10 分钟）

### 4.1 准备有 Bug 的代码

```bash
# 创建有问题的代码
cat > buggy_code.py << 'EOF'
def divide_numbers(a, b):
    return a / b

def get_user_age(users, user_id):
    return users[user_id]['age']

# 测试
print(divide_numbers(10, 0))
print(get_user_age({'1': {'age': 25}}, '2'))
EOF
```

### 4.2 让 Claude 修复

```bash
claude .
```

输入：
```
分析 buggy_code.py 的问题，并修复所有 Bug：
1. 添加异常处理
2. 添加输入验证
3. 使其更健壮
```

**期望行为：**
- Claude 识别除零错误
- Claude 识别键不存在的错误
- 提供修复方案并更新文件

**验证：**
```bash
exit
python3 buggy_code.py

# 应该不再崩溃
```

**✅ 通过 / ❌ 失败：** _______

---

## 🔌 测试 5：MCP 功能（10 分钟）

### 5.1 添加文件系统 MCP

```bash
# 添加 filesystem MCP
claude mcp add filesystem -- npx @modelcontextprotocol/server-filesystem $(pwd)

# 期望输出：Successfully added MCP server 'filesystem'
```

**✅ 通过 / ❌ 失败：** _______

---

### 5.2 添加 Git MCP

```bash
# 添加 Git MCP
claude mcp add git -- npx @modelcontextprotocol/server-git --repository $(pwd)

# 期望输出：Successfully added MCP server 'git'
```

**✅ 通过 / ❌ 失败：** _______

---

### 5.3 列出 MCP 服务器

```bash
# 列出所有 MCP
claude mcp list

# 期望输出：应该显示 filesystem 和 git
```

**实际输出：** ______________

**✅ 通过 / ❌ 失败：** _______

---

### 5.4 测试 MCP 功能

```bash
claude .
```

输入：
```
使用 Git MCP 查看当前仓库状态
```

**期望行为：**
- Claude 使用 Git MCP 查询状态
- 显示未提交的文件

**✅ 通过 / ❌ 失败：** _______

继续输入：
```
提交所有文件，提交信息："测试提交"
```

**验证：**
```bash
exit
git log --oneline -1

# 应该看到 "测试提交"
```

**✅ 通过 / ❌ 失败：** _______

---

## 💬 测试 6：会话管理（5 分钟）

### 6.1 创建可追溯的会话

```bash
claude .
```

输入：
```
记住这个数字：42
```

Claude 确认后，输入：
```
现在创建一个文件 secret.txt，内容是我刚才告诉你的数字
```

**验证：**
```bash
exit
cat secret.txt

# 应该显示 42
```

**✅ 通过 / ❌ 失败：** _______

---

### 6.2 继续上次会话

```bash
# 继续最近的会话
claude --continue
```

输入：
```
你还记得我之前告诉你的数字吗？
```

**期望输出：**
- Claude 应该记得是 42

**✅ 通过 / ❌ 失败：** _______

---

### 6.3 恢复特定会话

```bash
# 查看所有会话
claude --resume

# 选择一个会话恢复
# 验证上下文是否保留
```

**✅ 通过 / ❌ 失败：** _______

---

## ⚡ 测试 7：非交互模式（5 分钟）

### 7.1 简单问答

```bash
# 测试非交互式输出
claude -p . "什么是递归？用一句话回答"

# 应该直接输出答案并退出
```

**✅ 通过 / ❌ 失败：** _______

---

### 7.2 代码审查

```bash
# 创建待审查代码
cat > review_me.py << 'EOF'
def calculate(x,y):
    result=x+y
    return result
EOF

# 使用 -p 模式审查
claude -p . "审查 review_me.py，指出代码规范问题"
```

**期望输出：**
- 指出变量命名
- 指出空格问题
- 给出改进建议

**✅ 通过 / ❌ 失败：** _______

---

### 7.3 管道输出

```bash
# 输出到文件
claude -p . "列出 Python 最佳实践的 5 条建议" > python-tips.txt

# 验证
cat python-tips.txt

# 应该包含 5 条建议
```

**✅ 通过 / ❌ 失败：** _______

---

## 📊 测试结果汇总

### 统计

```bash
# 创建结果统计
cat > test-summary.md << 'EOF'
## 测试执行总结

### 测试环境
- 操作系统: [填写]
- Claude 版本: [填写]
- Node.js 版本: [填写]

### 测试结果统计
- 总测试项: 18
- 通过: [填写]
- 失败: [填写]
- 跳过: [填写]
- 通过率: [计算]%

### 详细结果

#### ✅ 通过的测试 (列出编号)


#### ❌ 失败的测试 (列出编号)


#### ⚠️ 遇到的问题


### 性能数据
- 平均响应时间: [填写] 秒
- 最慢响应: [填写] 秒
- 最快响应: [填写] 秒

### 总体评价
[填写你的评价]

### 建议和改进
[填写建议]

EOF
```

---

## 🎯 期望结果

### 全部通过的标准

如果你完成了所有测试且全部通过，说明：
- ✅ Claude Code 安装正确
- ✅ API 配置正常
- ✅ 核心功能工作正常
- ✅ MCP 集成成功
- ✅ 会话管理正常

### 部分失败的处理

如果有测试失败：
1. 查看 [故障排查指南](00-故障排查指南.md)
2. 记录失败的具体信息
3. 尝试重新测试该项
4. 如仍失败，收集诊断信息

---

## 🧹 清理测试环境

测试完成后，可以删除测试目录：

```bash
# 返回上级目录
cd ~

# 删除测试目录（可选）
rm -rf ~/claude-code-test

# 或者保留以备将来参考
mv ~/claude-code-test ~/claude-code-test-$(date +%Y%m%d)
```

---

## 📈 性能基准

以下是参考性能数据（供对比）：

| 操作 | 期望时间 | 可接受时间 |
|-----|---------|-----------|
| 启动会话 | < 3s | < 10s |
| 简单回复 | < 5s | < 15s |
| 创建文件 | < 5s | < 10s |
| 代码生成 | < 10s | < 30s |
| Bug 修复 | < 15s | < 45s |
| MCP 添加 | < 3s | < 10s |

如果你的测试时间远超"可接受时间"，建议：
1. 检查网络连接
2. 使用更快的模型（haiku）
3. 减少项目上下文大小

---

## ✅ 完成检查清单

完成测试后，确认以下事项：

- [ ] 所有测试项已执行
- [ ] 测试结果已记录
- [ ] 问题已记录到 test-report.md
- [ ] 性能数据已收集
- [ ] （可选）清理了测试环境

---

## 📚 下一步

测试通过后，你可以：

1. **开始实际项目**
   - 阅读 [新项目工作流](05-新项目工作流.md)
   - 或 [老项目开发指南](06-老项目开发指南.md)

2. **学习进阶功能**
   - [MCP 配置指南](08-MCP配置指南.md)
   - [子代理使用](13-子代理使用.md)

3. **优化使用体验**
   - [最佳实践](09-最佳实践.md)
   - [快捷键速查](04-快捷键速查.md)

---

**祝测试顺利！** 🎉
